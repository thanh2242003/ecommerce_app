import 'package:flutter/material.dart';

class TopSellingScreen extends StatelessWidget{
  const TopSellingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('top selling Screen'),
      ),
    );

  }
}